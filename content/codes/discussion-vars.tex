\begin{lstlisting}[language=Python]
elements = 10**6 # Um milhão de simulações
weights = [50, 20, 10, 20, 30, 40] # Peso de cada valor
values = [-100, -500, 500, 400, 300, 200] # Valores
weightTotal = sum(weights) # Armazena os pesos somados
\end{lstlisting}
